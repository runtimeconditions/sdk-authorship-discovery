[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "runtimeconditions-sdk-kubernetes-pod-watcher"
version = "0.1.0"
description = "Kubernetes Pod watcher for Runtime Conditions SDK mapping research"
requires-python = ">=3.10"
dependencies = ["kubernetes==36.0.3"]

[tool.setuptools.packages.find]
where = ["src"]
